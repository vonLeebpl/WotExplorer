<div class="nav_menu">
    <nav class="" role="navigation">
        <div class="nav toggle">
            <a id="menu_toggle"><i class="fa fa-bars"></i></a>
        </div>

        <ul class="nav navbar-nav navbar-right">
            {% if is_granted('ROLE_USER') %}
                <li class="">
                    <a href="javascript:;" class="user-profile dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                        <img src="{{ app.user.emblem }}" alt="">{{ app.user.username }}
                        <span class=" fa fa-angle-down"></span>
                    </a>
                    <ul class="dropdown-menu dropdown-usermenu pull-right">
                        <li><a href="javascript:;">  Profile</a>
                        </li>
                        <li>
                            <a href="javascript:;">
                                <span class="badge bg-red pull-right">50%</span>
                                <span>Settings</span>
                            </a>
                        </li>
                        <li>
                            <a href="javascript:;">Help</a>
                        </li>
                        <li><a href="/logout"><i class="fa fa-sign-out pull-right"></i> Log Out</a>
                        </li>
                    </ul>
                </li>
            {% else %}
                <li class="">
                    <a href="/login_openid"> Login <i class="fa fa-sign-in"></i></a>
                </li>
            {% endif %}
                <li>
                    <div class="right">
                        <form id='searchclantag' method="GET" class="navbar-form" role="form" action="/searchclan">
                            <div class="form-group input-group">
                                <input type="text" class="form-control" name="clan_tag" placeholder="Search clan... min 2 letters" required pattern=".{2,}">
                                 <span class="input-group-btn">
                                    <button class="btn btn-secondary" type="submit"><span class="fa fa-group"></span></button>
                                 </span>
                            </div>
                        </form>
                    </div>
                </li>
                <li>
                    <div class="right">
                        <form id='searchaccount' method="GET" class="navbar-form" role="form" action="/searchaccount">
                            <div class="form-group input-group">
                                <input type="text" class="form-control" name="account_tag" placeholder="Search player... min 3 letters" required pattern=".{3,}">
                                 <span class="input-group-btn">
                                    <button class="btn btn-secondary" type="submit"><span class="fa fa-male"></span></button>
                                 </span>
                            </div>
                        </form>
                    </div>
                </li>
            </ul>
    </nav>
</div>
